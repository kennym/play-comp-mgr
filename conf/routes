# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                           Application.index

# Map static resources from the /app/public folder to the /public path
GET     /public/                                    staticDir:public

## Spanish routes ##############################################################
# Competition
GET     /concurso/{id}/duracion/?                   Competitions.duration

# Participants
GET     /participante/index/?                       Participants.index
POST    /participante/{id}/trabajo/subir/?          Participants.submitWork
GET     /participante/{id}/trabajo/ver/?            Participants.showWork

# Organizers
GET     /organizador/index/?                        Organizers.index
POST    /organizador/concurso/{id}/iniciar/?        Organizers.startCompetition
POST    /organizador/concurso/{id}/parar/?          Organizers.stopCompetition
GET     /organizador/participante/{id}/can_submit/? Organizers.canSubmit

# Judges
GET     /jurado/index/?                             Judges.index
GET     /jurado/evaluar/{id}/?                      Judges.evaluateWork

# Catch rest
*       /{controller}/{action}                      {controller}.{action}


# Import CRUD routes
GET     /admin/?                                    Admin.index
*       /admin                                      module:crud

# Import Secure routes
*       /                                           module:secure
